# generated from spec: zypper-declarative.spec.md sha256:18253550a5c3d3f1818f0380811cb5dbc98874828693e49fc9cd5cbc923303dd
# tests by: claude-opus-4-8
#
# Independent black-box test harness for the zypper-declarative CLI binary.
# Tests invoke the built binary at ../../zypper-declarative (BINARY-LOCATION:
# project-root, per the cli-tool deployment template) via std::process::Command
# and assert on stdout, stderr, and exit code. No implementation source is
# imported; the interface under test is the CLI.

[package]
name = "zypper-declarative-independent-tests"
version = "0.6.4"
edition = "2021"
license = "GPL-2.0-or-later"
publish = false

[[test]]
name = "cli"
path = "tests/cli.rs"

[dependencies]
